{
  "name": "unslop-extension",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "LinkedIn content quality / AI slop detector \u2014 Chrome extension",
  "scripts": {
    "build": "tsc --noEmit && npm run clean && vite build && npm run build:popup",
    "build:popup": "node scripts/build-popup.mjs",
    "dev": "npm run build:popup && vite build --watch --mode development",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "@types/node": "^22.20.3",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
